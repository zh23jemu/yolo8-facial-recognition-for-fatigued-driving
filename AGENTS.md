# 项目维护记录

## 项目目标

本项目围绕疲劳驾驶场景下的人脸状态识别展开，使用 YOLOv8、CBAM 注意力机制、PyTorch、Ultralytics 和 PyQt5 等技术完成模型训练、实验对比、系统演示和毕业论文材料整理。

## 技术栈

- Python、PyTorch、Ultralytics YOLOv8
- OpenCV、PyQt5
- YOLO 数据集配置与模型权重文件
- Word 论文文档、实验报告和交付压缩包

## 当前架构

- `src/train/`：模型训练入口。
- `src/infer/`：推理检测入口。
- `src/app/`：桌面演示程序。
- `src/utils/`：疲劳判定规则与 Ultralytics 兼容补丁。
- `configs/`：YOLO 数据集与模型配置。
- `docs/`：论文、答辩、实验和交付说明材料。

## 开发规范

- 保持最小修改，优先沿用现有结构。
- Python 执行统一使用项目 `.venv`。
- 大型数据集、权重、渲染截图和交付压缩包默认不纳入 Git。
- 论文 DOCX 修改后需要进行渲染检查，确认图表、公式、文字和页面布局正常。

## Current Status

已基于学校模板版论文生成按新意见修订稿，完成摘要、关键词、第一章至第六章、结论与展望、参考文献对应关系和表格排版的进一步修订。

## Recent Changes

- 新增 `thesis_draft_改写_按意见修订_改写_图表公式编号修订.docx` 作为本轮修订副本。
- 补充第二章 CBAM 公式的正文引用和公式编号“公式（2-1）”。
- 补充并统一图、表在正文中的引用表达，覆盖第 3 章、第 4 章、第 5 章和第 6 章相关图表。
- 更新 `.gitignore`，忽略本轮 DOCX 渲染检查产生的页面图片和拼图文件。
- 调整参考文献正文引用编号，使正文首次引用顺序与文末参考文献列表一致。
- 将参考文献数量从 17 条补充到 20 条，新增文献均围绕 YOLOv8 疲劳检测、复杂光照鲁棒性和疲劳判定规则相关内容。
- 检查并修正正文中不规范的图表引用表达，将“如下图 6-1 所示”调整为“如图 6-1 所示”。
- 将 `附件4：黑龙江工业学院毕业设计（论文）模板.doc` 转换为 DOCX 底版，并把 `基于 YOLOv8 与注意力机制的疲劳驾驶面部识别系0521.docx` 内容迁入学校模板，生成 `基于 YOLOv8 与注意力机制的疲劳驾驶面部识别系统_学校模板版.docx`。
- 已用 Word 原生导出 PDF 抽查模板版论文的封面、目录、正文表格、图片页和参考文献页。
- 新增 `基于 YOLOv8 与注意力机制的疲劳驾驶面部识别系统_学校模板版_按新意见修订.docx`，按导师新意见扩写并调整论文。
- 重写摘要结构与关键词，关键词统一为“疲劳驾驶检测；YOLOv8；CBAM注意力机制”。
- 扩写第 1 章研究背景、意义、研究现状与主要内容；第 2 章删除 ONNX Runtime 核心技术表述并强化技术适用性；第 3 章补充数据来源、不均衡分析和数据集不足；第 4 章补充疲劳判定阈值、训练参数和 CBAM 嵌入位置；第 5 章强化实验曲线、Yawn 类别和混淆矩阵分析；第 6 章补充系统实现细节。
- 将从第 1 章到参考文献前的纯正文中文字符扩充到约 15003，正文引用与参考文献列表保持 `[1]` 至 `[19]` 对应。
- 使用 Word 原生导出 PDF 完成 39 页版式 QA，删除尾部空白页，并检查表格未出现明显溢出或遮挡。
- 更新 `.gitignore`，忽略本轮 `docs/thesis_new_revision_check_*` 渲染检查产物。

## Next TODO

- 如需提交 Git，暂存本轮 DOCX 修订稿、`.gitignore` 和本文件后创建中文 Conventional Commits 提交。
- 若学校继续反馈论文格式意见，优先在当前图表公式编号修订稿基础上继续修改。
- 正式提交前确认是否需要保留本地参考文献修订前备份文件；该备份已被 `.gitignore` 忽略。
- 下次提交时纳入本次图表引用表达修订后的两个论文 DOCX 文件。
- 如需提交模板化论文，纳入学校模板版 DOCX、按新意见修订稿和模板转换底版；渲染检查目录、临时正文 DOCX 和 PDF 仅作本地 QA，不提交。

## Open Issues

- DOCX 渲染工具曾返回非零退出码；学校模板版论文后续优先以 Word 原生 PDF 导出结果作为视觉 QA 依据。
- 仓库中仍保留若干大型压缩包和模型文件，提交或推送前需要确认是否已被 Git 历史清理或正确忽略。
- 新增外文参考文献格式已按现有列表风格整理，但最终仍应以学院模板或导师要求为准。
- 本轮按新意见修订稿仍需人工确认封面个人信息、学院/专业/班级等字段是否填写完整。
- 若导师要求更高清的 PR/F1 曲线和混淆矩阵，应从 `runs/yolo/` 原始实验结果重新导出或替换论文内图片。

## Architecture Decisions

- 论文修订采用“保留原稿，另存阶段性副本”的方式，便于回退和比对。
- 渲染检查产物只用于本地 QA，不作为正式交付内容。
